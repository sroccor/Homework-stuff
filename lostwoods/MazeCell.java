/**
 * A MazeCell represents a single "square" in a maze.
 */
public enum MazeCell {
    /**
     * Indicates an open location in a maze.
     */
    OPEN,
    
    /**
     * Indicates a walled-in (closed) location in a maze.
     */
    WALL
}
